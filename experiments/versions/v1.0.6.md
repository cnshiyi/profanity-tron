# v1.0.6 - bilingual documentation

Date: 2026-06-21

## 中文

### 目标

把项目文档和发布内容改成中英双语，避免 GitHub Release、源码 README 和发布包 README 只有单语言。

### 修改

- 根目录 `README.md` 改为中英双语。
- `RELEASE_NOTES.md` 改为中英双语，供 GitHub Release 自动读取。
- `scripts/build-windows.ps1` 里的发布包 README 模板改为中英双语，保证换电脑重新编译后不会退回英文单语。
- 发布 zip 文件名改为带版本号的 `shiyi-v1.0.6.zip`，GitHub Actions 也上传同样格式的文件名。
- `CHANGELOG.md` 增加 v1.0.6 版本记录。

### 验证

- 重新构建 `dist/shiyi-v1.0.6.zip`。
- 确认 zip 内包含 `README.md`、`VERSION.txt`、`start.exe`、`shiyi.exe`、`kernels/`、`runtime/`。
- 确认包内 `README.md` 包含中文和 English 两部分。

## English

### Goal

Make project and release documentation bilingual Chinese/English so GitHub Release notes, source README, and packaged README are not single-language.

### Changes

- Converted root `README.md` to bilingual Chinese/English.
- Converted `RELEASE_NOTES.md` to bilingual Chinese/English for GitHub Release automation.
- Updated the package README template in `scripts/build-windows.ps1`, so rebuilding on another computer keeps the packaged README bilingual.
- Changed the release zip filename to the versioned name `shiyi-v1.0.6.zip`; GitHub Actions uploads the same filename format.
- Added the v1.0.6 entry to `CHANGELOG.md`.

### Verification

- Rebuilt `dist/shiyi-v1.0.6.zip`.
- Verified the zip contains `README.md`, `VERSION.txt`, `start.exe`, `shiyi.exe`, `kernels/`, and `runtime/`.
- Verified the packaged `README.md` contains both Chinese and English sections.
